"""Shared cross-agent services (e.g. embedding service, LLM client wrapper).

Empty in Module 1 - populated starting Module 2+ (e.g. `llm_client.py`,
`embedding_service.py`, `crawler_service.py`).
"""
