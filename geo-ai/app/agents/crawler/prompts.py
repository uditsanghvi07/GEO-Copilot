"""Prompt templates for the Website Crawler agent.

Not applicable yet: this agent is pure extraction (Playwright + BeautifulSoup)
with no LLM calls, so there are no prompts to define in Module 2. Kept as an
empty placeholder so every agent has the same `agent.py` / `service.py` /
`prompts.py` shape, and so a future module (e.g. LLM-based content summary
of a crawled page) has an obvious place to add prompts without restructuring.
"""
