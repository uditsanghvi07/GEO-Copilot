"""Prompt templates for the Play Store Analyzer agent.

Not applicable yet: this agent only fetches listing/review data via
`google-play-scraper` and computes non-AI heuristic signals - no LLM calls
happen in Module 2. Kept as an empty placeholder so every agent has the
same `agent.py` / `service.py` / `prompts.py` shape (Module 3's sentiment
analysis agent is the first to actually need prompts here).
"""
